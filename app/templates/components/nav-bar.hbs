<nav class="navbar navbar-fixed-top app-nav">
	<div class="container">
		<div class="row login-nav-row">
			<div class="col-sm-2">
				{{#link-to 'application' class="navbar-brand"}}<img alt="democlient" src="../static/ember/img/logo.png" height="80px"/>{{/link-to}}
			</div>
			<div class="col-sm-10">
				{{auth-bar}}
			</div>
		</div><!--end top row-->
		
		<div class="row">
			<div class="col-sm-2"></div>
			<div class="col-sm-10">
				<ul class="nav nav-tabs">
					<!-- navigation menu -->
					{{#link-to "register" tagName='li'}}
						<a {{bind-attr href="view.href"}}><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Register</a>
					{{/link-to}}
					{{#if auth.isLoggedIn}}
						{{#link-to "home" tagName='li'}}
							<a {{bind-attr href="view.href"}}><span class="glyphicon glyphicon-home" aria-hidden="true"></span> Home</a>
						{{/link-to}}
						{{#if auth.profile.roles.admin}}
							{{#link-to "admin" tagName='li'}}
								<a {{bind-attr href="view.href"}}><span class="glyphicon glyphicon-th-list" aria-hidden="true"></span> Admin</a>
							{{/link-to}}
						{{/if}}
					{{/if}}
				</ul>
			</div>
		</div>	
		
	</div>
</nav>